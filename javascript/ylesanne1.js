// Sten Soomre
// 2023-12-13
// Ülesanne  1

// Lõin muutajad, et neid pärast kuvada tüüpi.
var numbers = 4.2;              // Tüüp: number
var tekst1 = 'Will Code 4 food';// Tüüp: string
var tekst2 = "123";             // Tüüp: string
var truevar = true;             // Tüüp: boolean
var massiiv123 = [1, 2, 3];     // Tüüp: array
var falsevar = false;           // Tüüp: boolean
var truevartext = 'true';       // Tüüp: string
var funnyone;                   // Tüüp: undefined

// Vaatame kas mul oli õigus, nende muutujate tüüpide kohta ¯\_(ツ)_/¯ 
console.log(typeof(numbers));
console.log(typeof(tekst1));
console.log(typeof(tekst2));
console.log(typeof(truevar));
console.log(typeof(massiiv123));
console.log(typeof(falsevar));
console.log(typeof(truevartext));
console.log(typeof(funnyone));

